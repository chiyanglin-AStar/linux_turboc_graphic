#include <stdio.h>
int main() {
    int i_num;

    printf("Enter an integer: ");

    // reads and stores input
    scanf("%d", &i_num);

    // displays output
    printf("You entered: %d", i_num);

    return 0;
}
